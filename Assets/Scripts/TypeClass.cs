using System;

namespace AutomatedTestSystem
{
    public class TypeClass
    {
        public Type type;

        public TypeClass(Type newType)
        {
            type = newType;
        }
    }
}